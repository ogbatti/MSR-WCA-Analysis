# Invitation-only auth (option 3)
# Copy relevant keys into .streamlit/secrets.toml (local) or Streamlit Cloud secrets.

AUTH_ENABLED = "true"
AUTH_ADMIN_EMAIL = "you@unhcr.org"
AUTH_ADMIN_PASSWORD = "ChangeMe_now_8chars"
AUTH_ADMIN_NAME = "DIMA Admin"
APP_PUBLIC_URL = "https://dimawca.app"

# Email notifications on account creation (Office 365 example)
SMTP_HOST = "smtp.office365.com"
SMTP_PORT = "587"
SMTP_USER = "you@unhcr.org"
SMTP_PASSWORD = "app-password-or-mailbox-password"
SMTP_FROM = "DIMA WCA <you@unhcr.org>"
SMTP_STARTTLS = "true"

# Durable user store on Streamlit Cloud (recommended)
# Create a PRIVATE GitHub repo (or private path) that holds only users.json.
# Create a fine-grained PAT with Contents: Read and Write on that repo.
AUTH_GITHUB_TOKEN = "github_pat_..."
AUTH_GITHUB_REPO = "your-org/msr-wca-auth-store"
AUTH_GITHUB_PATH = "users.json"
AUTH_GITHUB_BRANCH = "main"

# Optional: pre-seed users (hashes only). Prefer creating users via Admin UI.
# [auth.users."colleague@unhcr.org"]
# email = "colleague@unhcr.org"
# name = "Colleague"
# role = "user"
# active = true
# password_hash = "$2b$12$..."
